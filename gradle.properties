# The current version of Minecraft the project will use
mc_ver=1.21.1

# Mod Info
mod_name=Waystones Model Replacer
mod_authors=["deokma"]
mod_id=waystones_model_replacer
mod_version=1.0.0
group=dev.deokma
license=MIT
credits=BlayTheNinth (Waystones)
description=Visually replaces the Waystones model, textures, runes and particles with a custom model. Cosmetic-only, no gameplay changes.

# Waystones / Balm versions this addon is built & tested against.
# These are the ONLY two lines you change to retarget a future Waystones release.
# (Used by fabric/build.gradle and neoforge/build.gradle to pull the dev-only API jars.)
waystones_version=21.1.34
balm_version=21.0.58

# Manifold
manifold_version=2025.1.14

# Gradle
org.gradle.jvmargs=-Xmx6G
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.java.installations.auto-download=false
